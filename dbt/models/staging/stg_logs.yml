models:
  - name: stg_logs
    description: Logs user data with json parsing and cleaning
    columns:
      - name: ip_address
        description: user's ip address
        data_tests:
          - not_null
      - name: user_event
        data_tests:
          - accepted_values:
              arguments:
                values: ['login', 'logouts']
