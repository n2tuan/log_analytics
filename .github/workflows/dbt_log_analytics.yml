name: dbt Build

on:
  push:
    branches:
      - main

jobs:
 build:

    runs-on: self-hosted

    steps:
    
      - uses: actions/checkout@v4

      - name: Show location
        run: pwd

      - name: Show files
        run: ls -la